defaults

        syslog off
        logfile -
        tls off
        connect_timeout 60
        host ${SMTP_HOST}
        port ${SMTP_PORT}
        auth off

account default

        from ${SMTP_FROM}

